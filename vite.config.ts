import { defineConfig } from 'vite';

export default defineConfig({
  // Relative base so the app works when served from a subdirectory,
  // e.g. https://username.github.io/langlearn/
  base: './',
});
